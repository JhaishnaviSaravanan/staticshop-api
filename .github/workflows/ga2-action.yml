name: GA2 Simple Action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 24f1002342@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Done step
        run: echo "Workflow completed successfully"
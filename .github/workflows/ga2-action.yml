name: GA2 Simple Action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ✅ CACHE STEP (required)
      - name: Cache dependencies
        id: cache-eefd449
        uses: actions/cache@v4
        with:
          path: |
            ~/.npm
          key: cache-eefd449

      # ✅ PRIME STEP (required name)
      - name: prime-cache-eefd449
        run: echo "Cache hit? ${{ steps.cache-eefd449.outputs.cache-hit }}"

      - name: 24f1002342@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Done step
        run: echo "Workflow completed successfully"